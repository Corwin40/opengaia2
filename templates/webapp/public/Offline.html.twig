{% extends 'base.html.twig' %}

{% block meta %}
    {{ parent() }}
{% endblock %}

{% block title %}<title>{{ parameter.titleSite }} | Hors-ligne</title>{% endblock %}

{% block content %}
<div class="section">
    <div class="row">
        <div class="col-12">
            <h1>{{ parameter.nameSite }} </h1>
            <h4>{{ parameter.description }}</h4>
            <hr>
            <p>Notre site est actuellement en cours de développement.</p>
        </div>
    </div>
</div>
{% endblock %}
